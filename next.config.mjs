/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'img.freepik.com', 'static.vecteezy.com'],
    },
};

export default nextConfig;
